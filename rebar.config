{ sub_dirs, ["rel"] }.

{ lib_dirs, ["deps"] }.

{ deps, [
  { 'estatsd', ".*", { git, "git://github.com/squidfunk/estatsd.git", { branch, master } } }
]}.

{ erl_opts, [
  fail_on_warning, 
  debug_info
]}.

{ clean_files, ["ebin/*.beam"] }.

{ cover_enabled, true }.